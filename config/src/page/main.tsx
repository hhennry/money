import React from 'react'
import ReactDOM from 'react-dom/client'
import { Home } from './home'

ReactDOM.createRoot(document.getElementById('pages')!).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
)
